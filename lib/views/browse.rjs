pagination = render_record_select(:partial => 'pagination', :locals => {:page => @page})
list = render_record_select(:partial => 'list', :locals => {:page => @page})

page << %|Element.replace($$('##{record_select_id} div.pagination')[0], "#{pagination.gsub(/\n/, '').gsub(/"/, '\"')}")|
page << %|Element.replace($$('##{record_select_id} ol')[0], "#{list.gsub(/\n/, '').gsub(/"/, '\"')}")|